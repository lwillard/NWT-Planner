# -*- coding: utf-8 -*-
# Placeholder: keep the API expected by slide_rule_widget.py.
# Replace with your real implementation as needed.
def get_overpressure_radiis(yield_kt: float, height_m: float):
    # Return dummy radii (psi -> meters) for now
    return {
        "1psi": 1000.0,
        "2psi": 800.0,
        "5psi": 500.0,
        "10psi": 300.0,
    }
